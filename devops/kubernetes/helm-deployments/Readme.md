Our production stack is described in the `production` helm chart.

The stack is automatically deployed using the Google Cloud Builder. This is quite buggy still but should trigger based 
on GitHub code push.